import LoginForm from "../../components/LoginForm";

const LoginPage = () => {
  return (
    <section>
      <h2>Inicia sesión en ApiGym</h2>

      <LoginForm />
    </section>
  );
};

export default LoginPage;
